// Jump throw
  alias "+jumpthrow" "+jump;-attack"
  alias "-jumpthrow" "-jump"
  bind ALT "+jumpthrow"

// Radar zoom in-out
  bind . "incrementvar cl_radar_scale 0.25 1.0 0.05"
  bind , "incrementvar cl_radar_scale 0.25 1.0 -0.05"

// Nade shortcut
  bind "z" "slot8"
  bind "x" "slot7"
  bind "c" "slot6"

// Clear decals + use
  bind "e" "r_cleardecals; +use"

// Knife left
  bind "1" "slot1; cl_righthand 1"
  bind "2" "slot2; cl_righthand 1"
  bind "3" "slot3; cl_righthand 0"

// Score net_graph
	alias +netg "net_graph 1;+showscores"
	alias -netg "net_graph 0;-showscores"
	bind TAB "+netg"

// Volume
	bind "UPARROW" "incrementvar volume 0 0.3 +0.015"
	bind "DOWNARROW" "incrementvar volume 0 0.01 -0.015"

echo "Binds loaded"